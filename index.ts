// Ejemplos de código para cada sección
document.getElementById(
  "string-example"
).innerText = `let str: string = "Hello, TypeScript!";`;
document.getElementById("number-example").innerText = `let num: number = 42;`;
document.getElementById(
  "boolean-example"
).innerText = `let bool: boolean = true;`;
document.getElementById(
  "array-example"
).innerText = `let arr: number[] = [1, 2, 3, 4, 5];`;
document.getElementById(
  "object-example"
).innerText = `let obj: { name: string, age: number } = { name: "Alice", age: 30 };`;
document.getElementById("null-example").innerText = `let nul: null = null;`;
document.getElementById(
  "undefined-example"
).innerText = `let und: undefined = undefined;`;
document.getElementById(
  "function-example"
).innerText = `function sum(a: number, b: number): number { return a + b; }`;
